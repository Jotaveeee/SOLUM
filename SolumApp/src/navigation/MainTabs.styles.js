import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tabBar: {
    height: 72,
    paddingTop: 8,
    paddingBottom: 8,
    backgroundColor: '#F3F4F6',
    borderTopWidth: 0,
    elevation: 0,
    shadowOpacity: 0,
  },

  tabBarLabel: {
    fontSize: 12,
    marginTop: 2,
  },
});